.ui-card {
  background: var(--color-surface, #ffffff);
  border-radius: var(--radius-lg, 20rpx);
  padding: var(--space-6, 24rpx);
  box-shadow: var(--shadow-sm, 0 4rpx 18rpx rgba(17, 24, 39, 0.05));
  display: flex;
  flex-direction: column;
  gap: var(--space-4, 16rpx);
}
